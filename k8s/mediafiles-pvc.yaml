apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homelab-hub-mediafiles
  namespace: homelab-hub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
